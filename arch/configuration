LOG_FILENAME=${HOME}/arch-install

if [ ! -t 1 ]; then
    exec 3>&1
    exec 1>${LOG_FILENAME}.out.log 2>${LOG_FILENAME}.err.log
fi


title() {
    printTitle "$@"
}

printTitle() {
    echo "$@" >&3
}

printOption() {
    echo "      * ${@}" >&3
}
