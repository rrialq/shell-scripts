LOG_FILENAME=${HOME}/arch-install.log

if [ -t 1 ]; then
    exec 3>&1
    exec &> ${LOG_FILENAME}
fi

title() {
    printTitle "$@"
}

printTitle() {
    echo "$@" >&3
}

printOption() {
    echo "      * ${@}" >&3
}
